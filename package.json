{
  "name": "sistema-alquiler-cancha",
  "version": "1.0.0",
  "description": "Sistema de alquiler de canchas sintéticas",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"blue,green\" \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "kill": "node scripts/kill-ports.js",
    "kill:ports": "node scripts/kill-ports.js",
    "setup": "npm run install-all",
    "init-db": "cd server && node database/init-admin.js"
  },
  "keywords": ["canchas", "alquiler", "reservas"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

